{
  "name": "meisterwulf",
  "version": "0.1.0",
  "private": true,
  "description": "meisterwulf.com — ajutine avaleht",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
